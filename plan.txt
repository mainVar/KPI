
+1)инициализация. №порта. т. окно для команда в ручную 
-+2) список всех нужных команд 
stdr :
Pa (PB), E0, F2, KA0, KF0, RG0, U0__(U7__), H0, N0, Q0, W3, Y1   
output with
output without alpha header
cyclical temperature measurement
values b same as in channel A
switchover between measurement and calibration mode : only commands for calibration are valid
x0 --- reset command for commands x3/x4
x1--- trigger command (get)
x2---triggered command + storage of measured  value as reference value 
x3--- settting command for continuous triggering measurement upon a service request 
x4---settting command for continuous triggering
x8--- trigger command for both measurment channels (measured values are searated by delimiters ( corresponding to w0 to w8)

z0--- output of reference value 
z1 ---output of reference impedance 
z2--- output of reference frequency 
z3--- output of reference attenuation

Выход с
Выход без альфа-заголовка
Циклическое измерение температуры
Значения b такие же, как в канале A
Переключение между режимами измерения и калибровки: действуют только команды для калибровки
X0 --- команда сброса для команд x3 / x4
X1 --- команда триггера (get)
X2 --- инициированная команда + сохранение измеренного значения в качестве эталонного значения
X3 - команда настройки для непрерывного запуска измерения по запросу службы
X4 --- команда настройки для непрерывного запуска
X8 --- команда триггера для обоих каналов измерения (измеренные значения обжигаются разделителями (соответствующими w0-w8)
Z0 --- выход опорного значения
Z1 --- выход опорного импеданса
Z2 --- выход опорной частоты
Z3 --- выход опорного затухания


+3) интерфейс , кнопка выбора канала , введения количества измерений 
	+3.1)функция сборки команды по параметрам из интерфейса 
   	+	3.1.1 активный пробл , активный канал, скорость измерения .
	+3.2)функция подщета длинны команды для передачи значения одлинне в C_GPIBDevice.ibwrt  (int32)
4) (кусково, прим 1000 и один кусок ет 100  )2 масива данных или буфер для хранения данных с временной задержкой между командами чтения записи
5) вывод в файл .+
6) Графика
7) Окно действий tbx_Msg.AppendText -+
//-----------------------------------------------
tuz
//-----------------------------------------------
1) Разные периоды измерения
2) 2 канала ,измерения в 2-х канальном режиме +-
3) таблица № |дата|Канал A| Канал B |
 3.1) Ползунок таблицы всегда внизу(временный вариант)  (при новом измерении )+
 3.2) выравнивания таблицы -
4) мануал для пользователя  (кнопка юзверя)-